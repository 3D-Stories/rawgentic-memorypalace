__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
*.egg
.pytest_cache/
*.old-backup
.coverage
claude_docs/

# Local docker-compose config for the frontend. frontend/.env.example is the
# tracked template; the real .env carries machine-specific paths and UID/GID.
frontend/.env
