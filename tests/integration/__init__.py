"""Integration tests for rawgentic-memorypalace.

Tests in this package require either a running server subprocess
(concurrent writes, acceptance criteria) or test hooks against
non-existent servers (graceful degradation, timeouts).
"""
